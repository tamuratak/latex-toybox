export enum ScrollMode {
    UNKNOWN = -1,
    VERTICAL = 0, // Default value.
    HORIZONTAL = 1,
    WRAPPED = 2,
    PAGE = 3,
}

export enum SpreadMode {
    UNKNOWN = -1,
    NONE = 0, // Default value.
    ODD = 1,
    EVEN = 2,
}
